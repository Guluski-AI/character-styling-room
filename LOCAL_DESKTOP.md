# 角色造型室：本地个人版

## 当前架构

```text
现有网页界面
  ├─ 本地文件夹衣柜（最多索引 500 张）
  ├─ 多张穿搭参考选择
  └─ 异步任务状态轮询
        ↓
127.0.0.1 本地服务
  ├─ 模型适配器：Codex CLI
  └─ 存储适配器：本地文件夹
        ↓
ChatGPT 登录额度 + 本机结果目录
```

网页、本地服务、模型和存储彼此隔离。未来可增加 OpenAI API 或其他存储适配器，不需要重做页面。

## 自用流程

1. 保持 ChatGPT 桌面应用已安装，并确认 Codex CLI 使用 ChatGPT 登录。
2. 启动「角色造型室」桌面应用。
3. 点击顶部 Codex 状态可重新检测、测试连接；自动检测失败时可手动选择
   Codex CLI 可执行文件。手动路径会保存在应用数据目录的 `settings.json`。
4. 点击「选择文件夹」，连接已经分类整理的穿搭参考目录。
5. 上传角色图，选择一张或多张本地参考图，并按需添加自定义造型标签。
6. 生成 3 套结构化穿搭方案。
7. 只对选中的方案生成预览图，节省图片生成额度；生成后可通过
   「单独保存图片」复制到任意本地位置。

每次生成会建立独立运行目录，保存：

- `request.json`：本次输入
- `outfits.json`：3 套结构化方案
- `analysis-last-message.json`：Codex 原始结构化输出
- `*-preview.png`：选中方案的生成预览图
- `*-image-last-message.txt`：图片落盘结果

桌面版默认将这些文件保存在系统的应用数据目录，不会写回用户的衣柜原图目录。

## 打包与迁移

- `npm run desktop:dev`：先重新构建网页，再启动 Electron 桌面调试版。修改
  `app/page.tsx` 或 `app/globals.css` 后，重新运行此命令即可看到最新版本。
- `npm run desktop:pack`：生成当前 Mac 架构的未签名 `.app`
- `npm run desktop:zip`：生成便于复制到另一台 Mac 的 ZIP
- `npm run desktop:dmg`：生成未签名 DMG

把应用复制到另一台电脑后，页面和本地存储可直接使用。真实 AI 功能仍要求目标电脑：

- 已安装可用的 ChatGPT/Codex CLI
- 已完成 ChatGPT 登录
- 可以访问 Codex 服务

桌面版默认按 ChatGPT 应用内置位置、常见安装位置和系统 `PATH` 自动查找
Codex CLI。若另一台设备的安装位置不同，可在顶部 Codex 状态面板中手动
选择可执行文件，之后无需重复填写。

当前验证机器是 Apple Silicon，因此本轮生成 arm64 包。Intel Mac 或 Windows 需要分别构建对应架构。

## 本地端口

- `43127`：模型与文件服务
- `43128`：桌面网页

两者只监听 `127.0.0.1`，不会对局域网开放。长耗时分析和生图使用任务编号轮询，不依赖一条持续数分钟的 HTTP 长连接。
